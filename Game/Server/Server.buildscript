[project:Server]
type = exe
if(Windows)
{
    subsystem = Console
}
sources = src/**/*.cpp
headers = src/**/*.h src/**/*.hpp ../shared/**/*.hpp
includes = src, ../shared/
target_link_libraries(
    PRIVATE Engine
)
multiprocessor = true
std = 20
utf8 = true