[solution]
name = test
platforms = x64, Linux

include = ${ENGINE_PATH}/Engine/Engine.buildscript

[project:test]
type = dll
sources = src/**/*.cpp
headers = src/**/*.hpp
includes = src
target_link_libraries(
    PRIVATE Engine
)
std = 20
utf8 = true
